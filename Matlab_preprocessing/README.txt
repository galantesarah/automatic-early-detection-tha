Folder containing the Pre_processing_rx.m code and the functions it calls during execution.  MATLAB Image Processing Toolbox is required.

For it to run:

1. set the source folder on line 12

2. set the destination folder on line 53

Line to invert colors:
bw2 = imcomplement(bw);